namespace Kason.Net.Common
{
    using System;
    using System.Collections.Generic;

    public static class MapperHelper
    {
        //private static IMapping Mapping = new EmitMapping();

        //public static TTo Map<TTo>(object source)
        //{
        //    if (source == null)
        //    {
        //        return default(TTo);
        //    }
        //    return (TTo) Mapping.Map(source, source.GetType(), typeof(TTo));
        //}

        //public static TTo Map<TFrom, TTo>(TFrom source)
        //{
        //    return Mapping.Map<TFrom, TTo>(source);
        //}

        //public static IEnumerable<TTo> Map<TFrom, TTo>(IEnumerable<TFrom> sourceCollection)
        //{
        //    return Mapping.Map<TFrom, TTo>(sourceCollection);
        //}

        //public static object Map(object source, Type fromType, Type toType)
        //{
        //    return Mapping.Map(source, fromType, toType);
        //}

        //public static object Map(object source, object target, Type fromType, Type toType)
        //{
        //    return Mapping.Map(source, target, fromType, toType);
        //}
    }
}

