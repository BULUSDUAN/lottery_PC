namespace Kason.Net.Common
{
  //  using EmitMapper;
   // using EmitMapper.MappingConfiguration;
    using System;
    using System.Collections.Generic;

    internal class EmitMapping : IMapping
    {
        //public TTo Map<TFrom, TTo>(TFrom source)
        //{
        //    return ObjectMapperManager.DefaultInstance.GetMapper<TFrom, TTo>().Map(source);
        //}

        //public IEnumerable<TTo> Map<TFrom, TTo>(IEnumerable<TFrom> sourceCollection)
        //{
        //    return ObjectMapperManager.DefaultInstance.GetMapper<TFrom, TTo>().MapEnum(sourceCollection);
        //}

        //public object Map(object source, Type fromType, Type toType)
        //{
        //    return ObjectMapperManager.DefaultInstance.GetMapperImpl(fromType, toType, DefaultMapConfig.Instance).Map(source);
        //}

        //public object Map(object source, object target, Type fromType, Type toType)
        //{
        //    return ObjectMapperManager.DefaultInstance.GetMapperImpl(fromType, toType, DefaultMapConfig.Instance).Map(source, target, null);
        //}
    }
}

